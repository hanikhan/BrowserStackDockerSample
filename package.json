{
  "name": "browserstack-docker-example",
  "version": "1.0.0",
  "description": "This demonstrates running a basic selenium test on BrowserStack via Docker container using bs-local.com instead of localhost",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose up --build",
    "stop": "docker-compose down",
    "test": "./test.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserstack-local": "1.3.0",
    "selenium-webdriver": "3.4.0",
	"connect" : "*",
	"serve-static" : "*"
  }
}
